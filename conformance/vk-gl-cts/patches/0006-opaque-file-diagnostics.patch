PS5 OpenGL - upstream integration patch.
PS5 modifications by BlackBearReloaded, 2026.
Upstream copyrights and per-file licenses remain unchanged.
See THIRD_PARTY_NOTICES.md for source and license references.

diff --git a/framework/qphelper/qpDebugOut.c b/framework/qphelper/qpDebugOut.c
--- a/framework/qphelper/qpDebugOut.c
+++ b/framework/qphelper/qpDebugOut.c
@@ -167,7 +167,7 @@ void printRaw(MessageType type, const char *message)
 
     if (type == MESSAGETYPE_ERROR)
     {
-        putc('\n', out);
+        fputc('\n', out);
         fflush(out);
     }
 }
@@ -194,7 +194,7 @@ void printFmt(MessageType type, const char *format, va_list args)
 
     if (type == MESSAGETYPE_ERROR)
     {
-        putc('\n', out);
+        fputc('\n', out);
         fflush(out);
     }
 }
