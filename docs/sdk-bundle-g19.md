# Legacy SDK packaging profile: G19

This local-only 1080p60 profile contains the nonzero-mip copy and color-blit
channel-mapping fixes. Its frozen runtime has separate targeted and
204-execution sampled evidence, not a new full CTS campaign.

It is not a current download. Use [SDK 0.2.0](release-g62.md), or consult a
historical archive's embedded provenance, checksums and validation JSON when
reproducing that package. Do not mix libraries or combine different binaries'
test results.
